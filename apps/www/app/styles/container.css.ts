import { style } from '@vanilla-extract/css';

export const containerStyle = style({
  maxWidth: 780,
  margin: 'auto',
  padding: 24,
  height: '100%',
});

export const edgeStyle = style({
  paddingTop: 0,
  paddingBottom: 0,
});
